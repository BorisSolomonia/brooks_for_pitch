spring:
  datasource:
    url: ${PINS_DB_URL}
    username: ${PINS_DB_USER}
    password: ${PINS_DB_PASSWORD}
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.spatial.dialect.postgis.PostgisPG10Dialect
    open-in-view: false
  flyway:
    locations: classpath:db/migration

server:
  port: ${SERVER_PORT:${PINS_SERVICE_PORT:8084}}

auth0:
  issuer-uri: ${AUTH0_ISSUER_URI}
  audience: ${AUTH0_AUDIENCE}

brooks:
  cache:
    ttl-seconds: ${REDIS_CACHE_TTL_SECONDS}
  social:
    base-url: ${SOCIAL_SERVICE_URL:http://${SOCIAL_SERVICE_HOST:social-service}:${SOCIAL_SERVICE_PORT:8082}}
  lists:
    base-url: ${LISTS_SERVICE_URL:http://${LISTS_SERVICE_HOST:lists-service}:${LISTS_SERVICE_PORT:8083}}
  notifications:
    base-url: ${NOTIFICATIONS_SERVICE_URL:http://${NOTIFICATIONS_SERVICE_HOST:notifications-service}:${NOTIFICATIONS_SERVICE_PORT:8087}}
    reveal-title: ${BROOKS_NOTIFICATION_REVEAL_TITLE:A hidden memory was revealed!}
    reveal-body: ${BROOKS_NOTIFICATION_REVEAL_BODY:You discovered a memory nearby}
    reveal-type: ${BROOKS_NOTIFICATION_REVEAL_TYPE:PIN_REVEALED}
  proximity:
    bucket-size-deg: ${BROOKS_BUCKET_SIZE_DEG}
  cleanup:
    enabled: ${BROOKS_CLEANUP_ENABLED}
    batch-size: ${BROOKS_CLEANUP_BATCH_SIZE}
    retention-days: ${BROOKS_CLEANUP_RETENTION_DAYS}
    cron: ${BROOKS_CLEANUP_CRON}
  web:
    origins: ${BROOKS_WEB_ORIGINS}

# Internal service-to-service authentication
internal:
  service:
    name: ${INTERNAL_SERVICE_NAME}
    key: ${INTERNAL_SERVICE_KEY}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,circuitbreakers
  health:
    circuitbreakers:
      enabled: true

logging:
  level:
    root: INFO
    com.brooks.pins: INFO
    org.springframework.web: INFO

resilience4j:
  circuitbreaker:
    configs:
      default:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
        waitDurationInOpenState: 5s
        failureRateThreshold: 50
    instances:
      socialService:
        baseConfig: default
        waitDurationInOpenState: 10s
      listsService:
        baseConfig: default
        waitDurationInOpenState: 10s
      notificationsService:
        baseConfig: default
        waitDurationInOpenState: 10s
  retry:
    configs:
      default:
        maxAttempts: 3
        waitDuration: 500ms
    instances:
      socialService:
        baseConfig: default
      listsService:
        baseConfig: default
      notificationsService:
        baseConfig: default
  timelimiter:
    configs:
      default:
        timeoutDuration: 2s
    instances:
      socialService:
        baseConfig: default
      listsService:
        baseConfig: default
      notificationsService:
        baseConfig: default
