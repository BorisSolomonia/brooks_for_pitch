# Database Schemas

Each microservice owns its schema under `services/<service>/db/schema.sql`.

`db/schema.sql` and `db/legacy_monolith_schema.sql` are legacy monolith artifacts and not used by the microservices.
